{
  "name": "litedoc-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "tauri": "tauri",
    "desktop:dev": "scripts\\with-vsdev.cmd npm run tauri -- dev",
    "desktop:build": "scripts\\with-vsdev.cmd npm run tauri -- build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "dompurify": "^3.2.7",
    "marked": "^16.4.1",
    "mermaid": "^11.12.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "typescript": "^5.9.3",
    "vite": "^7.2.4"
  }
}
